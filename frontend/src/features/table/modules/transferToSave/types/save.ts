export interface ITransferToSave {
	id: string
	instrumentId: string
	dateStart: number
	dateEnd: number
	notesStart: string
	notesEnd: string
	created: string
}

export interface ITransferToSaveDTO {
	id: string
	instrumentId: string
	dateStart: number
	dateEnd: number
	notesStart: string
	notesEnd: string
}
